{
  "name": "hermes-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r typecheck && pnpm -r build",
    "test": "pnpm -r test",
    "test:live": "pnpm --filter @hermes/llm run test:live",
    "lint": "biome check .",
    "dev": "tsx watch apps/hermes/src/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.5",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
